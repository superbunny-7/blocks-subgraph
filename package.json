{
  "name": "blocks-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create": "graph create --node http://localhost:8020/ conflux/blocks-subgraph",
    "remove": "graph remove --node http://localhost:8020/ conflux/blocks-subgraph",
    "deploy": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:8020 --version-label 1.0.0 conflux/blocks-subgraph", 
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.59.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
